
spring.datasource.url=jdbc:postgresql://localhost:5432/taskdata
spring.datasource.username=postgres
spring.datasource.password=PN1807@data
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update  
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Image Configuration

# ✅ Maximum file size (e.g., 2MB)
spring.servlet.multipart.max-file-size=6MB

# ✅ Maximum request size (for all files in a form)
spring.servlet.multipart.max-request-size=15MB

# ✅ Enable multipart support (default is true)
spring.servlet.multipart.enabled=true

server.port=8080
